export const headerHeight = 70;

export const imageSize = 200;

export const imageCount = 3;

export const maxImageSize = imageSize * imageCount;

export const imageGridGap = 21;

export const thumbnailSize =
  (maxImageSize - imageGridGap * (imageCount - 1)) / 3;
